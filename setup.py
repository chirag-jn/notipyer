from setuptools import setup, find_packages

setup (
    name = 'notipyer',
    version = '0.1.1',
    description = 'Notification API for Python',
    long_description = 'Async email notifications using SMTP',
    author = 'Chirag Jain',
    packages = find_packages(),

    install_requires = []
)